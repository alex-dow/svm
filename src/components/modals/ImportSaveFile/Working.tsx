/**
 * Expected to be in a <Dialog/>
 */

import { ProgressSpinner } from "primereact/progressspinner";

export default function Working() {
    return (
    <div className="flex items-center">
        <ProgressSpinner />
    </div>
    );
}